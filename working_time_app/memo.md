確定プロンプト：勤務時間計算アプリ（Vanilla JS版）
1. 構成ファイル

以下の3ファイルを作成し、それぞれの役割を明確に分離してください。

    index.html

    style.css

    script.js

2. UI/UX 仕様

    背景デザイン: 清潔感のある「白基調のオフィス」をイメージしたデザイン。背景画像の上に透過した白い入力カードを中央配置すること。

    入力項目: 「開始時刻」「終了時刻」「休憩時間」「前日までの累積残業時間」の4つ。

    入力方式: 各項目は 4桁の数字（例: 0900） で入力する。

    表示結果: 以下のパターンをリアルタイム、または計算ボタン押下で表示。

        時間形式: 「〇時間 〇分」

3. ロジック仕様（script.js）

    時刻変換: 4桁の文字列を「時」と「分」に分け、合計の「分」に変換して計算する。

    日またぎ対応: 終了時刻が開始時刻より数値として小さい場合（例: 2200開始、0500終了）、終了時刻に24時間を加算して計算すること。

    計算式:　前日まで累積残業時間　- (終了時刻 - 開始時刻) - 勤務時間（8時間）　-　休憩時間 = 累積残業時間（時分）

    バリデーション: 下2桁が60分以上の場合はエラー、または計算不可とする処理を含める。

4. メンテナンス性への配慮

    マジックナンバーを避け、定数や関数を分かりやすく命名すること。

    HTMLとJSの紐付けは id 属性を使用し、ロジックは DOMContentLoaded 内で初期化すること。
